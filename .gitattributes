# Golden-test fixtures are compared byte-for-byte against the pretty-printer's
# LF output. Git's Windows default (autocrlf) would otherwise check these out
# with CRLF and fail every golden test on Windows CI, so pin them to LF on all
# platforms.
test/golden/*.txt text eol=lf
